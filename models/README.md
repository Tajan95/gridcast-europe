# Modellartefakte

Hier liegen später lokal gespeicherte Modelle, Preprocessing-Pipelines und Metadaten. Große Binärdateien werden nicht in Git versioniert; stattdessen werden Trainingscode, Parameter und Datenstand dokumentiert.

